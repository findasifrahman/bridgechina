[build]
builder = "NIXPACKS"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @bridgechina/api db:generate"

[deploy]
startCommand = "cd apps/api && pnpm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

